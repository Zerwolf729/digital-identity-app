import 'package:flutter/material.dart';

class AppColors {
  // Primary accent
  static const Color primary = Color(0xFF60A5FA);
  static const Color primaryDark = Color(0xFF3B82F6);

  // Dark theme backgrounds
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color surfaceSecondary = Color(0xFF2A2A2A);

  // Text
  static const Color textPrimary = Color(0xFFE5E5E5);
  static const Color textSecondary = Color(0xFFBDBDBD);
  static const Color textMuted = Color(0xFF9E9E9E);

  // Borders and dividers
  static const Color border = Color(0xFF3A3A3A);
  static const Color divider = Color(0xFF2E2E2E);

  // Accent
  static const Color accent = Color(0xFF8B5CF6);

  // Status
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);

  // Icons
  static const Color icon = Color(0xFFBDBDBD);

  // Skill tags
  static const Color skillBg = Color(0xFF2A2A2A);
  static const Color skillText = Color(0xFFE0E0E0);

  // Badge
  static const Color badgeBg = Color(0xFF1E3A8A);
  static const Color badgeText = Color(0xFF93C5FD);

  // Media overlay
  static const Color mediaOverlay = Color(0x4D000000);

  // Contact icon background
  static const Color contactIconBg = Color(0xFF2A2A2A);
}
